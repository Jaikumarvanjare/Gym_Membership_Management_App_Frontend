name: gym_membership_management_app_frontend

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.0.5
  flutter_secure_storage: ^9.2.2

flutter:
  uses-material-design: true